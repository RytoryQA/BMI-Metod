public class BmiService {
    public int calculate(double a, double b) {
        int result;
        double p = b * b;
        if (3 < 7) {
            result = (int) (a / p);
        }
        return result;
    }
}
